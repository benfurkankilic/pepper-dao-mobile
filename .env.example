# Environment Variables for Pepper DAO Mobile
# Copy this file to .env and fill in your actual values
# Note: The Project ID is a public identifier (not a secret) but keeping it in .env
# allows for easy configuration across different environments

# Reown AppKit Project ID (Public Identifier)
# Get your Project ID from https://dashboard.reown.com
# This is a public identifier used in client-side code - safe to include in app bundles
# However, keep it in .env for easy configuration management across environments
EXPO_PUBLIC_REOWN_PROJECT_ID=your_reown_project_id_here
EXPO_PUBLIC_CHILIZSCAN_API_KEY=your_chiliscan_api_key